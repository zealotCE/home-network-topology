PORT=3000
HOST=0.0.0.0
VITE_API_BASE_URL=/api
TOPOLOGY_DATA_DIR=./apps/backend/data
TOPOLOGY_DB_PATH=
TOPOLOGY_CONFIG_PATH=./config.example.yaml

# Router SSH keys should be mounted outside the repo/image. This env var points
# at the mounted private-key path used by config.example.yaml.
OPENWRT_IDENTITY_FILE=
