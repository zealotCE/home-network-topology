PORT=3000
HOST=0.0.0.0
VITE_API_BASE_URL=/api
TOPOLOGY_DATA_DIR=./apps/backend/data
TOPOLOGY_DB_PATH=
TOPOLOGY_CONFIG_PATH=./config.example.yaml

# Router credentials are resolved from env vars named in config.example.yaml
# or entered in the UI. Do not store secret values in YAML.
OPENWRT_PASSWORD=
OPENWRT_IDENTITY_FILE=
